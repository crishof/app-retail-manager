spring:
  application:
    name: retail-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: "${LOCAL_SPRING_CONFIG_IMPORT:${SPRING_CONFIG_IMPORT:optional:configserver:http://localhost:8088}}"