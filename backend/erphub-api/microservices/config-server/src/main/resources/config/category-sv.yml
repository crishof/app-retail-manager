server:
  port: 9003

spring:
  cloud:
    discovery:
      enabled: true
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:${POSTGRES_NETWORK_HOST:${POSTGRES_LOCAL_HOST:localhost}}}:${POSTGRES_PORT:${POSTGRES_CONTAINER_PORT:${POSTGRES_HOST_PORT:5544}}}/${CATEGORY_DB:category_db}
    username: ${POSTGRES_USER:admin}
    password: ${POSTGRES_PASSWORD:root1234}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 600000
      connection-timeout: 20000
  rabbitmq:
    host: ${RABBITMQ_HOST:${RABBITMQ_NETWORK_HOST:${RABBITMQ_LOCAL_HOST:localhost}}}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_DEFAULT_USER:guest}
    password: ${RABBITMQ_DEFAULT_PASS:guest}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  openfeign:
    circuitbreaker:
      enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}}
    register-with-eureka: true
    fetch-registry: true

logging:
  level:
    org.springframework: ${LOG_LEVEL_SPRING:INFO}

management:
  tracing:
    sampling:
      probability: 1.0
  health:
    circuitbreakers:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,refresh,circuitbreakers,circuitbreakerevents,metrics
  endpoint:
    health:
      show-details: always


resilience4j:
  circuitbreaker:
    instances:
      supplier-sv:
        registerHealthIndicator: true
        eventConsumerBufferSize: 10
        failureRateThreshold: 50
        minimumNumberOfCalls: 5
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 5s
        permittedNumberOfCallsInHalfOpenState: 3
        slidingWindowSize: 10
        slidingWindowType: COUNT_BASED
  retry:
    instances:
      supplier-sv:
        maxRetryAttempts: 5
        waitDuration: 10s
  ratelimiter:
    instances:
      supplier-sv:
        limitForPeriod: 10
        limitRefreshPeriod: 5s
        timeoutDuration: 10s
        registerHealthIndicator: true
        eventConsumerBufferSize: 100
  timelimiter:
    instances:
      supplier-sv:
        timeoutDuration: 2s
        cancelRunningFuture: true
      ImageFeignClientuploadImageMultipartFileString:
        timeoutDuration: 30s
        cancelRunningFuture: true
      ImageFeignClientreplaceImageMultipartFileStringString:
        timeoutDuration: 30s
        cancelRunningFuture: true
      ImageFeignClientdeleteImageStringString:
        timeoutDuration: 15s
        cancelRunningFuture: true
  bulkhead:
    instances:
      suppliers-sv:
        maxWaitDuration: 5s
        maxConcurrentCalls: 5

default:
  message: Hi