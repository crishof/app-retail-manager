<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- =========================================================
         Parent POM identity (Multi-module Root)
    ========================================================= -->
    <groupId>com.crishof</groupId>
    <artifactId>erphub-api</artifactId>
    <version>0.0.1</version>
    <packaging>pom</packaging>

    <name>erphub-api</name>
    <description>ERP Hub Microservices Parent</description>

    <!-- =========================================================
         Modules
         Cada microservicio debe agregarse aquí para ser parte del build
    ========================================================= -->
    <modules>

        <module>retail-api</module>

        <!-- =========================================================
             Legacy Modules (Infraestructura compartida y API Gateway que se migrarán gradualmente)
              - Se recomienda migrar cada uno a su propio módulo independiente
              - Mantenerlos aquí mientras se migran, luego eliminarlos uno por uno
        ========================================================= -->
        <module>api-gateway</module>
        <module>config-server</module>
        <module>service-registry</module>

        <!-- =========================================================
             Legacy Modules (Microservicios existentes que se migrarán gradualmente)
              - Se recomienda migrar cada uno a su propio módulo independiente
              - Mantenerlos aquí mientras se migran, luego eliminarlos uno por uno
        ========================================================= -->
        <module>branch-sv</module>
        <module>cash-sv</module>
        <module>category-sv</module>
        <module>customer-sv</module>
        <module>exchange-sv</module>
        <module>identity-sv</module>
        <module>image-sv</module>
        <module>inventory-sv</module>
        <module>location-sv</module>
        <module>pricing-sv</module>
        <module>product-sv</module>
        <module>purchase-sv</module>
        <module>reporting-sv</module>
        <module>sales-sv</module>
        <module>supplier-catalog-sv</module>
        <module>supplier-sv</module>
<!--        <module>brand-sv</module>-->
<!--        <module>tenant-sv</module>-->
    </modules>

    <!-- =========================================================
         Properties (centralized versions)
         Se recomienda que ningún módulo declare versiones directamente
    ========================================================= -->
    <properties>

        <!-- ===============================
        Build Encoding / Compiler Metadata
        ================================ -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.parameters>true</maven.compiler.parameters>

        <!-- ===============================
             Java / Compiler
        ================================ -->
        <java.version>25</java.version>

        <!-- ===============================
             Spring Platform
        ================================ -->
        <spring-boot.version>4.0.6</spring-boot.version>
        <spring-cloud.version>2025.1.1</spring-cloud.version>

        <!-- ===============================
             Libraries
        ================================ -->
        <lombok.version>1.18.46</lombok.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <springdoc.version>2.8.8</springdoc.version>
        <jjwt.version>0.12.7</jjwt.version>

        <!-- ===============================
             Quality Tools
        ================================ -->
        <jacoco.version>0.8.14</jacoco.version>
        <sonar-maven-plugin.version>4.0.0.4121</sonar-maven-plugin.version>

    </properties>

    <!-- =========================================================
         Dependency Management (BOM imports + managed libs)
         Permite que los módulos NO declaren versiones explícitas
    ========================================================= -->
    <dependencyManagement>
        <dependencies>

            <!-- ===============================
                 Spring Boot BOM
            ================================ -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ===============================
                 Spring Cloud BOM
            ================================ -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ===============================
                 MapStruct
            ================================ -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
                <scope>compile</scope>
            </dependency>

            <!-- ===============================
                 Springdoc OpenAPI
            ================================ -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- ===============================
                 JJWT (JWT)
            ================================ -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- =========================================================
         Build / Plugin Management
         Plugins compartidos por todos los módulos.
         Los módulos solo deben declararlos si necesitan configuración extra.
    ========================================================= -->
    <build>
        <plugins>
            <!-- Activa compilación con annotation processors gestionados en pluginManagement -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>

                <!-- ===============================
                     Maven Compiler Plugin
                     Incluye Lombok + MapStruct processors
                ================================ -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${java.version}</release>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>

                <!-- ===============================
                     Spring Boot Maven Plugin
                ================================ -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <executions>
                        <execution>
                            <id>repackage</id>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- ===============================
                     JaCoCo (coverage)
                ================================ -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                </plugin>

                <!-- ===============================
                     Sonar Scanner
                ================================ -->
                <plugin>
                    <groupId>org.sonarsource.scanner.maven</groupId>
                    <artifactId>sonar-maven-plugin</artifactId>
                    <version>${sonar-maven-plugin.version}</version>
                </plugin>

            </plugins>
        </pluginManagement>
    </build>

</project>